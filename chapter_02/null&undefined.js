
// undefined mean variable are defined but not assign any value to that variable.

let undefinedSample;
console.log(undefinedSample);

// Null mean variable are defined and initialize with null value.

let nullSample = null;
console.log(nullSample);